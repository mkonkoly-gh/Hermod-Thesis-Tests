# Baseline profile for the real Pi 5 hardware. Kustomize base differs
# (uses overlays/dev-hardware which passes through USB dongles and talks
# to the in-cluster Zigbee2MQTT). Resource limits match the 8GB profile
# since that's what the Pi actually has physically.
name: baseline-pi5-live
description: Real Pi 5 8GB hardware; dev-hardware overlay; USB passthrough.
inherits_from: baseline-kind-8gb
hardware_target: pi5-live
claims: [C1, C4, O3, O5]
