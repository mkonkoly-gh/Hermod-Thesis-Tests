# Pi 5 8GB saturate-10k-flat with the nanomq broker doubled in CPU + memory.
# Hypothesis: pass3-6 + the bottleneck census across 30/33 v3 runs all
# point at the broker (not coord) as the throughput wall. Doubling the
# broker should shift the ingested ceiling well past the current ~12.5k/s
# plateau if that hypothesis is correct.
#
# Used by: master-cron Phase B' (bigbroker stress 25k/50k/75k/100k) +
# any ad-hoc HERMOD_OVERRIDE_RATE run pointed at this profile.
name: saturate-10k-flat-bigbroker-pi5-live-8gb
description: Pi 5 8GB saturate-10k-flat with 2× nanomq cap (200/1000m CPU, 128/512Mi mem) — broker-bottleneck verification.
inherits_from: saturate-10k-flat-pi5-live-8gb
hardware_target: pi5-live
claims: [C1, O5]
resources:
  nanomq:
    requests: { memory: "128Mi", cpu: "200m" }
    limits:   { memory: "512Mi", cpu: "1000m" }
