# Note

The campaign figure uses all runs and does not account for race conditions, cache effects, C# JIT warm-up, or regular flakiness of `podman wait`.

For per-profile detail, the figures generated under `../raw/` are recommended; for the cleaned, cross-run aggregates, see the thesis figures.
